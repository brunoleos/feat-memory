# Configuração local do Claude Code
.claude/

# Artefatos de build/install do Python
__pycache__/
*.egg-info/
build/
dist/

# Virtualenvs criados pelo desenvolvedor
.venv/
venv/

# Estado transiente do deploy quando rodado neste próprio repositório
# (dogfood opcional). Em projetos consumidores, o bloco é injetado
# automaticamente pelo `agent-memory deploy` em sentinel block.
.agent-memory-deploy/
