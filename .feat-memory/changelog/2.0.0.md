---
version: 2.0.0
date: 2026-06-26
---

# 2.0.0 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Mudado (BREAKING)

- **Estado e histórico redesenhados.** `STATE.md` e o event-sourcing de checkpoints (ADR-0018/0019) saem; o foco em-voo vive em `changelog/UNRELEASED.md` e o histórico em `changelog/<tag>.md` (pasta imutável por-tag, ADR-0042); o orçamento de retomada é derivado das refs das entradas-bullet (ADR-0043). `feat-memory release` congela o UNRELEASED e cria a tag da versão corrente (ADR-0045). (F-0035, F-0036, F-0037)
- **Keep-a-Changelog deprecado.** O `CHANGELOG.md` monolítico na raiz dá lugar à pasta `changelog/` + `INDEX.md` gerado (ADR-0042).

## Removido

- `checkpoints.py`, os comandos `checkpoint`/`state-rebuild`, `STATE.md`, `CHANGELOG.md` (raiz) e `migrate --to=checkpoints`. A feature F-0015 (state-from-checkpoints) foi deprecada.
