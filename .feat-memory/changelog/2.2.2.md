---
version: 2.2.2
date: 2026-06-26
---

# 2.2.2 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Corrigido

- **`patch_agents_frontmatter` casa o nome de pasta legado `.agent-memory/`** — antes a regex só casava `.feat-memory/STATE.md`, dando **no-op silencioso** em projetos pré-rename. Agora casa qualquer `…/STATE.md`, normaliza `.agent-memory/`→`.feat-memory/` nas referências, e é escopado ao frontmatter. (F-0037)
- **Fim do log falso-positivo no `deploy`:** `patch_agents_frontmatter` retorna a **lista do que de fato mudou**, e o deploy loga só isso. Antes o deploy afirmava "references.state → unreleased" mesmo quando o patch dava no-op (a remoção do `state_max_bytes` ligava um booleano único). (F-0037)
