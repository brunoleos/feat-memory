---
version: 2.2.0
date: 2026-06-26
---

# 2.2.0 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Adicionado

- **Funil único do futuro: `.feat-memory/ideas.md`** — o `suggestions.md` meta-only vira o funil de **qualquer** ideia futura (produto + meta), com **triagem por tipo** na debrief (capacidade→Feature `proposed`; decisão→ADR `proposed`; sistema→aplica; senão descarta). Resolve a ambiguidade que confundiu o agente do cliente e a falta de tracker. Nova seção "O futuro" no METHODOLOGY. (F-0038, ADR-0047)

## Mudado (BREAKING — auto-migrado pelo `deploy`)

- **Vocabulário do futuro unificado:** o status de entrada de Feature passa de `planned` para **`proposed`** (mesmo nome do ADR) — pipeline único `ideia → proposed → realizando → realizado`. `feat-memory deploy` migra `planned`→`proposed` e `suggestions.md`→`ideas.md` automaticamente (rode-o antes do audit). Supersede parcial do ADR-0046 → ADR-0047 (funil) + ADR-0048 (retrospectiva re-afirmada). (F-0038, F-0039, ADR-0047, ADR-0048)
