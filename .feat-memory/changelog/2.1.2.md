---
version: 2.1.2
date: 2026-06-26
---

# 2.1.2 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Corrigido

- **`.gitattributes` normaliza EOL dos artefatos da metodologia** (`text eol=lf` em `.feat-memory/**`, `AGENTS.md`, `CLAUDE.md`) — mata o churn "CRLF will be replaced by LF" em working trees Windows. (F-0035)
- **`migrate --to=changelog` patcheia o frontmatter do AGENTS.md** no cutover: `references.state`→`unreleased` e remove `state_max_bytes`, que o `deploy` (só refresca o bloco entre sentinelas) deixava defasados após a migração. (F-0037)
