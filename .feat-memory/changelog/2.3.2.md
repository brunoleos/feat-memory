---
version: 2.3.2
date: 2026-06-30
---

# 2.3.2 — 2026-06-30

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Adicionado

- **Guard estrutural `test_no_stale_cutover_refs` + ritual (ADR-0049)** contra a classe de bug "stale-on-cutover" que recorreu: rename/remoção de símbolo passa a exigir varredura exaustiva por `grep` (início e fim) + um teste que falha se uma superfície **shipada** (skills/templates/data) citar comando/arquivo removido. Pega a regressão antes de shipar, sem depender do dogfood do cliente. (ADR-0049)

## Corrigido

- **`archive` quebrado no layout 2.x (bug funcional).** Lia o `STATE.md` (removido) para saber quais features estão ativas (e excluí-las do arquivamento) — no 2.x isso virava conjunto vazio, e o comando arquivaria features ainda **em voo**. Agora deriva o active do `changelog/UNRELEASED.md` (com fallback para um `STATE.md` legado). Teste novo. (ADR-0043)
- **Varredura exaustiva pós-cutover — ~10 superfícies com referência morta ao STATE/checkpoint/suggestions limpas:** `schema_reference` (a doc gerada documentava `STATE.md` → agora `changelog/UNRELEASED.md` + nota de legado), `telemetry` (label "STATE.md lido"), `indexing` (header do archive INDEX), `pre-commit` hook (comentários), template `STATE.md` morto **deletado**, docstrings (`deploy`/`changelog`/`__init__`/`parsing`), e a `memory-debrief` que ainda mandava rodar o `migrate --to=changelog` removido. (ADR-0049)
