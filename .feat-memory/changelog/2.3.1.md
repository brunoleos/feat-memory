---
version: 2.3.1
date: 2026-06-30
---

# 2.3.1 — 2026-06-30

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Corrigido

- **Gates de commit atualizados para o layout 2.x (bug bloqueante do tensegrams).** `check-doc-sync-staged` (hard) e `check-staleness-staged` (soft) reconheciam só `STATE.md`/manifest/decisions e nomeavam o `STATE.md` removido — bloqueando commits **docs-only** legítimos. Agora: (a) creditam `changelog/UNRELEASED.md` como registro do trabalho; (b) **`AGENTS.md` passa a ser doc, não código** (era a causa-raiz — um commit só de `AGENTS.md` + `ideas.md` disparava o gate). Mesma correção em `validate_state_freshness` e nos labels do crosscheck do audit. 2 testes novos (incl. regressão do caso AGENTS.md+ideas.md). (F-0032)
