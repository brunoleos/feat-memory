---
version: 2.1.1
date: 2026-06-26
---

# 2.1.1 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Mudado

- **METHODOLOGY.md atualizado para o modelo novo** — `changelog/UNRELEASED.md` no lugar do STATE, checkpoints removidos, retrospectiva + backlog (fecha a coerência da reforma; ADR-0042/0043/0046). Removido `state_max_bytes` órfão dos templates de frontmatter.

## Corrigido

- **`migrate --to=changelog` preserva a prosa de Current/Next do STATE** ao semear o `UNRELEASED.md` — antes herdava só as refs `active_*`, perdendo o foco real da sessão. (F-0037)
- **`released_versions` lê a pasta `changelog/`** (além de `CHANGELOG.md` legado e git tags), corrigindo o cross-check de status pós-cutover. (F-0035, ADR-0042)
