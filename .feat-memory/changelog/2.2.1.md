---
version: 2.2.1
date: 2026-06-26
---

# 2.2.1 — 2026-06-26

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Corrigido

- **`deploy` refresca o header do `ideas.md`** preservando as entradas do usuário. Antes, um `ideas.md` migrado mantinha o header velho (meta-only, ADR-0046), contradizendo as skills 2.2.0 e perpetuando a confusão que o funil deveria matar. Agora o header é conteúdo de metodologia, refrescado a cada deploy (como o bloco do AGENTS.md). (F-0038)
- **`deploy` patcheia o frontmatter do AGENTS.md** (`references.state`→`unreleased`, remove `state_max_bytes`) — antes só o `migrate` fazia, deixando projetos já-migrados com o frontmatter defasado. Fecha a lacuna para quem migrou antes da 2.1.2. (F-0037)
