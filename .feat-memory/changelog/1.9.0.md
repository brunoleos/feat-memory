---
version: 1.9.0
date: 2026-06-26
---

# 1.9.0 — 2026-06-26

Comando de migração do layout legado para o novo (aditivo; o cutover roda na 2.0.0).

### Adicionado

**F-0037 (in_progress) — `feat-memory migrate --to=changelog`.** Migra o layout legado para o novo: split do `CHANGELOG.md` monolítico por versão em `changelog/<v>.md`, move o `[Unreleased]` + as refs ativas do `STATE.md` para `changelog/UNRELEASED.md`, regenera o `INDEX` e remove os legados (`CHANGELOG.md`, `STATE.md`, `checkpoints/`). Idempotente — re-rodar num layout já migrado é no-op. Lógica em `changelog.py::migrate_to_changelog_folder`. 4 testes novos.
