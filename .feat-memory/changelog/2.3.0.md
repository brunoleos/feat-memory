---
version: 2.3.0
date: 2026-06-30
---

# 2.3.0 — 2026-06-30

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Removido

- **Máquina de migração one-time (1.x→2.x) retirada** — não há mais projetos a migrar (decisão do mantenedor; não vale carregar cruft). Saem: `feat-memory migrate --to=changelog` (`migrate_to_changelog_folder` + helpers), os auto-migrations do `deploy` (`migrate_planned_to_proposed`, `patch_agents_frontmatter`, rename `suggestions→ideas`) e o teste de migração. Feature F-0037 deprecada. **Mantidos:** o refresh de header do `ideas.md` no deploy (ongoing) e a migração de folder `.agent-memory/`→`.feat-memory/` (ADR-0039, separada). (F-0037)

## Corrigido

- **Logs do `deploy` honestos (#2 do dogfood):** os ramos "sem mudança" do bloco do AGENTS.md e do `.gitattributes` deixavam de dizer "(bloco atualizado)" — ambíguo, parecia que tinham atualizado. Agora dizem "já em dia (sem mudanças)". Fecha o tema do log falso-positivo que começou na 2.2.2.
